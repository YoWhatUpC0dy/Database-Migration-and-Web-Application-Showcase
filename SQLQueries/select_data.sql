-- SQL Query to Select Employee Information
-- In this SQL script, I retrieve employee data from the 'Employees' table.
-- You can see the real data in action.
-- This query returns details about employees with their IDs, first names, last names, and hire dates.
SELECT EmployeeID, FirstName, LastName, HireDate
FROM Employees;
